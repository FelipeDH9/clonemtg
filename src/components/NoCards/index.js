import './styles.css'

function NoCards() {
  return (
    <div className="no-cards">
      <p>
        Nenhuma carta para ser mostrada, filtre pelo nome ou clique no botão
        para mostrar todas as cartas.
      </p>
    </div>
  )
}

export default NoCards
